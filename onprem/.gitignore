# Postgres bind-mount data — never commit
data/

# Local secrets
.env
.env.local

# Local compose overrides and dev-only config (machine-specific)
docker-compose.override.yml
local-datasources.yml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
